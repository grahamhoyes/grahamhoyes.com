---
title: "A Test Post"
date: "2023-01-01"
tags: ["writings", "book", "reflection", "foo bar"]
draft: false
summary: "Once upon a time, there was a human with a mission: to write
  code that actually works on the first try. Many believed that this was
  only a myth. As it turns out, they were right."
---

export const Bar = ({ children }) => {
  <>
    # Hello there
    <div>{children}</div>
  </>;
};

Hello there

- This is a list of cool things

export const Foo = () => {
  return <># Hello</>;
};
